# Naming migration

The internal research prototype used names such as `BiSAM2` and
`Baseline_Bi_SSCCE`. The public implementation is named **BiVSS-CD** and exposes
only `bivss_cd.BiVSSCD`. The experimental names are intentionally not available
as compatibility aliases.
